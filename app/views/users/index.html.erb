<h2>User's list</h2>

<%= flash_message %>

<% if(@users.any?) %>
  <ul>
    <% @users.each do |user| %>
      <li><%= user.name %> | <%= link_to("Products", user_products_path(user)) %></li>
    <% end %>
  </ul>
<% end %>

<%= link_to("New user", new_user_path) %>